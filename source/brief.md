# Project brief

<!-- TEMPLATE: the setup skill fills this in during the first session.
     If you are the AI and this file still contains TODO markers, run
     .claude/skills/setup/SKILL.md before doing anything else. -->

## Who

- **Owner**: TODO (name, what they do, how technical they are)
- **The project**: TODO (one paragraph: what this site/brand is, for whom)
- **Audience**: TODO (who visits, in what situation, deciding what)

## The site

- **Domain**: chaire-bopa.com
- **Live URL**: https://www.chaire-bopa.com
- **Languages**: French (only)
- **Pages**: `index` (homepage, in order: hero, context band, `#notre-mission`, `#innovations-bopa`, `#notre-vision` (standalone horizontal Innovation→Acceptabilité→Interopérabilité→Soutenabilité→Progrès journey, same navy as Notre mission), `#qui-sommes-nous` (a rich chapter with 3 subsections only - la Chaire/recherche, l'environnement clinique du CHB, l'équipe (`#equipe` lives inside it); the "Ils nous soutiennent"/mécénat subsection moved out as of 2026-08-27), `#nous-contacter` (real email/address/LinkedIn de la Chaire, Web3Forms), `#nous-soutenir` (now the last editorial section before the footer: 3 real foundation-logo cards + 2 real patron-logo cards + a donation panel with the real AP-HP link and the validated hand-heart visual)), `innovations` (all 14 projects, tabbed by category: préopératoire/peropératoire/postopératoire/médico-économique - the definitive list lives in `site/src/data/projects.js`), `boptech` (the BOPtech platform), `publications`. As of 2026-08-27 none of these three dedicated pages have a header nav entry any more (owner's explicit call) - they're still live, just reachable only by direct URL or from links inside the home page's own content.
- **Chaire contact facts (2026-08-27, owner-confirmed)**: general contact email `charles.hellec@aphp.fr`; postal address Hôpital Paul-Brousse, 12 avenue Paul Vaillant Couturier, 94804 Villejuif Cedex; the Chaire's own LinkedIn company page (distinct from personal team member profiles) is `https://www.linkedin.com/company/chaire-innovation-bopa/?viewAsMember=true`. Real AP-HP donation link: `https://soutenir.fondationaphp.fr/BOPA/`.
- **Collections**: TODO (any repeated content: gallery, products, projects, events; where its data file lives)

## Derivatives

- **Decks**: TODO (list decks under site/public/decks/, each with its purpose and audience)
- **Apps**: TODO (list apps under apps/, each with what it does and where it's published)

## Governance

<!-- Filled during setup, in plain terms; revisit whenever someone new joins. -->

- **Who owns the accounts**: GitHub org `Chaire-BOPA`, repo `agentic-organization` (public). Cloudflare Pages was set up by a coworker.
- **Who can ask for changes**: at least two people so far - the coworker who originally set up the site, and Axelle Piguet (GitHub: axpiguet)
- **Who publishes**: team mode in practice, but still solo-style workflow - each person pushes directly to `main` via their own Claude session (not PRs). Worth revisiting per the README's "when you grow" note if this causes a real collision (one already happened: a coworker's commit landed while a session was mid-edit; resolved with a clean rebase, but it's a sign this repo now has more than one active editor). A second channel for the same risk: the owner sometimes uploads a file straight through GitHub's web UI (e.g. `source/brand/assets/graphics/phases.png`, 2026-08-25) instead of dropping it in `source/inbox/` - that lands as a commit on `main` mid-session too. `git fetch`/`pull --rebase` right before every push, not just at session start, catches both cases.
- **Access granted so far**: Axelle Piguet (GitHub: axpiguet) - admin, added 2026-08-17

## Current priorities

- Iterative homepage/site polish based on the owner's ongoing feedback (layout, copy, imagery) - most sessions right now are small, focused edits rather than big new builds.
- The owner has an established pattern for bigger visual pivots: export a handful of files (the homepage `.astro`, `tokens.css`, `site.css`, `Base.astro`) to a separate AI (ChatGPT) for a redesign proposal, then hand the result back as a zip with an integration doc for Claude Code to diff against the live project and merge carefully - not applied blindly. Expect this to recur; when it does, read the integration doc first, diff every file against current state before touching anything, and verify locally before publishing.
- HepatGreen, MesGardes, and "Évaluer la charge cognitive du chirurgien" now have real (if still short) owner-provided descriptions as of the 2026-08-27 V3 editorial pass - no longer an open fact gap. "Coût de l'hépatectomie" was removed from the validated project list the same day (owner's explicit call). The homepage's productivity numbers (publications/thèses/logiciels/start-up, living in the `#projets` and `#soutenir` stat rows) are confirmed real figures.
- **Open, awaiting the owner's decision (flagged 2026-08-27)**: on `/nos_innovations` (renamed from `/innovations` the same week - old URL 301-redirects), 10 of the 14 project cards (everything except LiverSight/BOPCAM/Goldfinger/Colette) now show a homogeneous blue SVG placeholder illustration instead of the site's older real photo for that project (e.g. PancreasSight, Henri, the greffons/compte-rendu/ressenti/pair photos) - a deliberate visual-consistency choice to match the reference mockup for this forms-only pass. The real photos are untouched on disk (`site/public/images/projets/`, `source/brand/assets/projets/`), just not displayed here for now. Revisit once real per-project visuals for the newer projects exist, or if the owner prefers the old photos back.

## History and decisions

The **detail** of how hypotheses and positioning evolved, and **why** (tied to each discussion),
lives in [`decisions.md`](decisions.md). Keep the milestones below; **after a discussion that moves
a hypothesis or the positioning, update `decisions.md`.**

<!-- Append dated one-liners for decisions worth remembering, newest first.
     Example: 2026-07-18 · dropped the blog idea, energy goes to the gallery. -->

- 2026-08-28 (thirty-septième pack, "02 Écosystème V1") · Rebuilt subsection 02 of "Qui sommes-nous ?" wholesale: replaced the dark navy "Un environnement clinique d'excellence" block with a light subsection "02. Un écosystème unique dédié à l'innovation", background derived via OKLCH from `--color-accent` (verified `#c7ddf9`, 11.6:1 contrast) - visibly more saturated than the site's one pale-blue token but still a light "BOPA" blue, not gray or navy. 2/3 content + 1/3 CHB photo desktop layout; 4 bullet points with cyan pictos and exact texts (158 m² and 500 mètres highlighted cyan, point 3 "expertise chirurgicale de classe mondiale" fully cyan as the strongest argument); 4 stats (1 500 actes au bloc / 180 hépatectomies / 50 pancréatectomies / 180 transplantations hépatiques/an) posed directly on the section background with no card/band at all, only thin separators - a new pattern distinct from subsection 01's navy stats band. Title hierarchy ("02." + title, same line) copied 01's exact type scale into new subsection-scoped classes rather than sharing 01's own classes. Used the CHB photo already present in the project from a prior session (`public/photos/centre-hepato-biliaire.png`) after flagging to the owner that the pack asked for a photo she'd supply herself - she confirmed using the existing one and publishing now. See `source/brand/design.md`.
- 2026-08-28 (sixth follow-up to "02 Écosystème", same day) · Fixed an em dash "—" that had slipped into the CHB photo caption ("Centre Hépato-Biliaire — Hôpital Paul-Brousse AP-HP"), replaced with the plain hyphen the existing voice guide already requires (`source/brand/voice.md` line ~27) - a missed instance of an already-documented rule, not a new one. Worth a quick scan for stray em dashes whenever new copy is written directly in a component rather than pulled from `source/content/`.
- 2026-08-28 (fifth follow-up to "02 Écosystème", same day) · Removed the trailing period after "02" in the subsection marker - subsection 01's own number ("01") never had one, so "02." was a small inconsistency introduced when the marker was first built. Quick text fix, no layout change.
- 2026-08-28 (fourth follow-up to "02 Écosystème", same day) · Photo enlarged again (533×327, was 477×295) by widening its grid share further, and subsection 02's own bottom padding tightened to match the value already used at the end of subsection 01, so the blue background now hugs the stats band the same way the previous subsection's background hugs its own last line. See `source/brand/design.md`.
- 2026-08-28 (third follow-up to "02 Écosystème", same day) · Owner asked to enlarge the CHB photo (there was room) and to widen the stats band so its right edge reaches the end of point 2's sentence ("...le bloc opératoire."). Photo grew ~28%/40% by widening its grid column share, not its aspect ratio, so the earlier no-aggressive-crop fix stays intact; the stats band's width now matches that specific sentence's rendered edge instead of an arbitrary fixed value. See `source/brand/design.md`.
- 2026-08-28 (second follow-up to "02 Écosystème", same day) · Owner delivered the definitive CHB photo via `source/inbox/` (now filed in `source/brand/assets/photos/` and `site/public/photos/`, inbox emptied). Its crop ratio changed from 4:3 to 16:9 so the wide real photo doesn't cut into the building's side towers; the photo now sits vertically centered on the points+stats block below the title (title itself moved out of the two-column grid); the stats band widened slightly (26rem→30rem) so the longest label wraps to 2 lines instead of 3, cells still equal width. See `source/brand/design.md`.
- 2026-08-28 (follow-up to the "02 Écosystème" pack, same day) · Fine-tuned subsection 02 right after publishing: background nudged one step lighter (`#c7ddf9`→`#cfe2f9`), the 4 point icons lost their white circle (pictos sit directly on the background now), the dead space below "De la recherche à l'impact"'s two cards was cut way down so the two subsections' backgrounds now meet almost seam-to-seam, the "02." title moved up tight under that seam, and the stats band was narrowed with its numbers truly centered per cell. See `source/brand/design.md`.
- 2026-08-28 (thirty-sixth pack, "correctifs compacts V3") · Compacted the whole sequence from the academic logos through the two impact cards: the navy stats band moved up (48px→20px gap above it) and the gap after it shrank (64px→36px) without touching the band's own dimensions/content/typography (verified unchanged); "De la recherche à l'impact" shrank to ~74% of its size and lost its cyan tick, now reading as a real sub-title instead of competing with the subsection's main H1; gaps to the intro and to the cards tightened too (34px→16px, 44px→28px). The two cards also got a very thin gold border (`--color-gold`, reusing the token already validated for the médico-économique category on `/nos_innovations` rather than inventing a new "champagne" shade) with a flat white background - everything else inside them (checks, links, separators) stays cyan. See `source/brand/design.md`.
- 2026-08-28 (thirty-fifth pack, second follow-up) · Left impact card was leaving ~65px of dead space at the bottom (its content was naturally shorter than the right card's, but both stretch to equal height in the grid) - fixed by making the card a flex column with its checklist growing to fill the space evenly. Also top-aligned both BOpEx/BOPTECH logos in their shared-height slot (was center-aligned) and trimmed that slot's height to remove the last bit of reserved slack, tightening the logo-to-description gap while keeping both descriptions starting at the same height. See `source/brand/design.md`.
- 2026-08-28 (thirty-fifth pack follow-up) · BOpEx logo reduced, BOPTECH logo enlarged, and a real bug fixed: the BOpEx/BOPTECH labels and descriptions weren't actually centered on their logos (the same "global `p{text-align:left}` beats inherited center" bug already documented once from the Vision pass in August - recurred because the rule wasn't re-checked when this new component was built). Both description paragraphs now also start at exactly the same height regardless of each logo's own size, via a shared fixed-height logo wrapper. See `source/brand/design.md`.
- 2026-08-28 (thirty-fifth pack, "impact-cards-exact") · Refinement pass on the two "De la recherche à l'impact" cards only (band/title/intro above frozen again, verified unchanged). Icon and title now share one header row per card (was stacked); titles gained "1./2." numbering; checklist result titles recolored cyan (were navy); dashed separators added between checklist items; BOpEx/BOPTECH logos grown substantially with a bold label above each and the "Découvrir le BOPEX ↗" link restored as a visible CTA (not just a bare logo link). See `source/brand/design.md`.
- 2026-08-28 (thirty-fourth pack, "De la recherche à l'impact V1") · New component added directly under the navy stats band (everything above it, band included, explicitly frozen per a `ZONE-FIGEE.txt` in the pack - verified unchanged after publishing). Two white bordered cards side by side: "Des résultats concrets au service du terrain" (a 4-item checklist - 2 start-ups, 2 logiciels médicaux, Colette, MesGardes - named in text only, no logos, since neither has a validated asset) and "Renforcer les liens entre l'AP-HP et les industriels..." (BOpEx linking to aphp.fr/bopex, BOPTECH with no link at all). Replaces the older, thinner "De la recherche au terrain"/"Faire sortir l'innovation du laboratoire" blocks, whose content this absorbed and expanded - their CSS was fully deleted, not left dead. The reference image used orange for the second card; collapsed to the same cyan as the first since orange isn't a validated BOPA color. See `source/brand/design.md`.
- 2026-08-28 (thirty-third pack, third follow-up) · Owner asked to raise the logo group further and align it horizontally with the band's "8" (4th) stat below. Raised the vertical nudge (-1rem→-1.75rem) and added a calibrated horizontal margin for the alignment - first attempt undershot by exactly half (a `margin-left` on a centered flex item only shows up as half its value visually, a real CSS behavior, not a mistake to second-guess next time), corrected once measured. Both offsets reset to 0 below the ~800px breakpoint, since the band wraps to 2×2 there and "align with its 4th column" stops making sense. See `source/brand/design.md`.
- 2026-08-28 (thirty-third pack, second follow-up) · Owner asked, verbally, for the logo group to shift slightly right and rise further (explicitly OK with visual overlap), and for the navy band to get shorter still - this time authorizing shrinking its internal icons/numbers too, since padding alone had reached its limit. Done: horizontal gap widened 32px→44px, a manual -1rem upward nudge added back (removed one pack ago per that pack's own "exact centering, no arbitrary offset" rule - now intentionally reopened), band padding/icons/numbers all reduced further. Also hit a real tooling gotcha mid-verification: the Browser tool's viewport briefly measured 0×0 after several preset switches, producing wildly wrong geometry numbers that were caught by checking `window.innerWidth` before trusting them - see `source/brand/design.md`.
- 2026-08-28 (thirty-third pack follow-up) · Owner immediately pushed back after the "finitions V4" publish, correctly: the logos hadn't actually moved closer to the text, and asked again for the band's height to shrink and its width to return to full content width (reversing that pack file's own stated constraints, in direct chat). Root cause found and fixed: the text column (`minmax(0,1fr)`) was absorbing the grid's full remaining width (952px) well past the paragraph's own 800px cap, leaving an invisible ~150px gap my own `gap`-property check hadn't caught - real distance from visible text to logo was 290px, not the reduced-looking number I'd verified. Capped the text column at `minmax(0,50rem)` (matching the paragraph's own max-width) so the grid box has no slack; true visible gap is now 138px. Band reverted to full content width and its padding reduced again for a genuinely shorter height. See `source/brand/design.md` for the verification-methodology lesson this exposed.
- 2026-08-28 (thirty-third pack, "finitions V4") · Third round on the same 3 spacing/position complaints in subsection 01, each pushed further: intro→"01" gap halved again (`var(--space-4)`, 32px); the co-porteurs logos now centered *exactly* (0px measured delta) against only the 2-paragraph text block per an explicit geometric rule the owner supplied (titre/01/section excluded) - the arbitrary optical nudge from the prior pack was removed per that rule, and the logos column switched from an fr-ratio to a fixed `12.5rem` width (matching the logos' own real max-width) to pull the group left without touching the text column; the navy band narrowed again, this time to a fixed `58rem` (928px, centered), verified to keep every label on one line, with its height/padding/pictos/numbers left untouched as instructed. Also found and fixed a real bug along the way: a CSS Grid `auto` track collapses to 0px when sized around a `loading="lazy"` image that hasn't loaded yet. See `source/brand/design.md`.
- 2026-08-28 (thirty-second pack, "finitions V3") · Another pure fine-tuning pass on subsection 01 (still no content/structure change). Left-aligned the "Qui sommes-nous ?" eyebrow to exactly match Notre Vision's own eyebrow treatment (title/intro stay centered); reduced the intro→"01" gap further using `var(--space-5)`, overriding the prior pass's finding that it already matched Mission (owner: still felt like too much once seen live, independent of Mission's own rhythm); nudged the 3 co-porteurs logos up slightly and closer to the text (an optical correction - the group was already mathematically centered against the text block, verified, but read as "too low" due to AP-HP's visually denser mark up top vs. Paris-Saclay's flat wordmark at the bottom); reversed the navy stats band back to full container width edge-to-edge (was narrowed to 90%/centered just one pack ago) with a slightly shorter height via padding only - band's internal pictos/numbers/labels explicitly left untouched this time. See `source/brand/design.md`.
- 2026-08-28 (thirty-first pack, "finitions V2") · Pure spacing/sizing pass on subsection 01, no content or structure change (owner explicit: "la structure actuelle me plaît"). Found and fixed a second real double-margin bug (eyebrow-to-title gap was 31px instead of Mission/Vision's real 13.6px); widened the Chaire text column enough to gain a line without shrinking the font (the fix was the paragraph's own `max-width`, not the grid ratio - widening the grid alone did nothing, the paragraph was already capped narrower); removed the text/logos divider; centered and enlarged the 3 co-porteurs logos; narrowed the navy stats band to 90% width, centered, with smaller icons/numbers. The section-level top padding and the intro→01 gap were both already exactly matching Mission/Vision (verified, not assumed) - left untouched rather than shrunk further, since the pack's own instruction was to reuse real values, not invent smaller ones. See `source/brand/design.md`.
- 2026-08-28 (thirtieth pack, "01 exact") · Owner supplied a literal reference image and asked for a close, "not just in spirit" reproduction of the upper part of subsection 01, stopping right after a new navy stats band (the rest of subsection 01 - BOpEx/BOPTECH/valorisation - left untouched, expected to be its own future pack, "la suite De la recherche à l'impact"). Brought back a large `01` marker with a thin divider to its title, added a matching thin divider between the co-porteurs text and logos, enlarged the logos again, replaced the plain academic-stats row with a rounded navy band (icon circles + white numbers/labels), and rewrote the presentation text (new second paragraph, "Pr Éric Vibert" the only cyan-highlighted word). Title and intro paragraph centered and their `max-width` tuned so both wrap to exactly 2 lines on desktop, matching the reference. Owner also said explicitly (2026-08-28) not to ask for publish confirmation any more, ever - see `CLAUDE.md`. See `source/brand/design.md`.
- 2026-08-28 (twenty-ninth pack, "V4 correctifs") · Subsection 01 pulled back toward sitewide consistency after the V3 mockup-match pass: fixed a real bug where `.about-v2` (carrying `.section-block`) was double-applying top padding, harmonized the "space before Qui sommes-nous ?" and "space before 01" to Mission's own real values, fixed a CSS-specificity bug that was silently forcing the big intro title onto the wrong (larger, generic) type scale, centered that title/intro, removed the V3 pass's network background image and the tick under `01` entirely (not replaced), removed "CO-PORTÉE PAR" so the 3 logos sit higher (and enlarged them slightly), introduced one shared heading level for the 3 internal sub-block titles, and shrank the academic/valorisation numbers to a smaller, already-existing scale (`.about-v2__clinical-stat`'s) after the V3 scale read as "too dashboard." See `source/brand/design.md`.
- 2026-08-28 (twenty-eighth pack, "V3") · Subsection 01 rebuilt a second time same day to match an approved reference image (`REFERENCE-VISUEL-CHAIRE.png`) closely rather than freely: co-porteurs logos (AP-HP/IMT/Paris-Saclay) moved into a 2-column layout beside the intro text (stacked vertically, never horizontal/carded), the `01` marker enlarged to near-title size (was shrunk small in the prior pack), all 7 big numbers (55/51/7/8, 2/2/1) resized to match the site's actual `.stat-row` figure scale exactly, and a decorative network-pattern image added top-right fading into the section's own pale background. Also added, sitewide: a small cyan tick under all 6 main section eyebrows (Mission/Innovations/Vision/Qui sommes-nous/Contacter/Soutenir), one shared CSS rule. See `source/brand/design.md`.
- 2026-08-28 (twenty-seventh pack) · "Qui sommes-nous" subsection 01 ("La Chaire d'excellence") rebuilt fluid/border-free per an externally-sourced pack: added the `QUI SOMMES-NOUS ?` eyebrow above the chapter, a discreet cyan `01.` marker, and rewrote the whole subsection's narrative (fondation 2020/Pr Éric Vibert → écosystème AP-HP/IMT/Paris-Saclay → 4 chiffres académiques (55 publications, 51 communications orales, 7 doctorants, 8 étudiants en Master) → BOpEx/BOPTECH → 3 chiffres de valorisation (2 start-up, 2 logiciels médicaux, 1 chatbot)) with zero border/divider CSS, replacing the old grid-of-boxes layout. BOpEx and BOPTECH now have real logo files in the repo for the first time (owner supplied them via `source/inbox/` mid-session) - BOpEx links to `aphp.fr/bopex`, BOPTECH intentionally has no link yet. 02 (CHB) and 03 (équipe) untouched, kept as a template for the same treatment later. See `source/brand/design.md`.
- 2026-08-28 (twenty-sixth pack) · Header anchor offsets corrected to the header's actual measured height rather than the old approximate 88px/84px values: new `--bopa-header-height` token (64px ≥480px wide, 60px below), based on the compact `.is-scrolled` header state since that's what's active by the time any anchor scroll lands. Single mechanism (`scroll-margin-top` on the 6 main ids), no added `scroll-padding-top` layered on top. See `source/brand/design.md`.
- 2026-08-27 (twenty-fifth pack, same day) · The prior pack's lightened filter navy (`color-mix` toward white) read as too gray - the owner asked for lighter *and* more saturated instead, which `color-mix` toward a neutral can't do. Rebuilt `--bopa-filter-navy` in OKLCH using the CSS relative-color syntax (lightness + chroma raised, hue pinned to the real navy). Also thickened MesGardes/HepatGreen's gold liseré (1px → ~1.5px) - discovered along the way that fractional `border-width` rounds to the nearest whole pixel and doesn't actually render thicker, so switched the liseré to `box-shadow` instead (which does render fractional widths), and had to combine it with the existing hover shadow so hovering doesn't hide it. See `source/brand/design.md`.
- 2026-08-27 (twenty-fourth pack, same day) · `/nos_innovations/` filters recolored to a new `--bopa-filter-navy` (the bloc-haut navy lightened ~7% via `color-mix`) instead of the exact navy, for the selected fill and unselected border/text - the main dark background itself untouched. Notre Vision: the three pillar subtitles now force exactly 2 lines each (explicit `<br/>` for Acceptabilité/Soutenabilité, width-only control for Interopérabilité's unchanged text), same 285px width for all three (up from 215px) - required widening the timeline slightly and re-tuning the arrow-to-circle gap and the tablet-layout breakpoint (moved 980px→1220px) to avoid a newly-introduced silent overflow-clipping bug. See `source/brand/design.md`.
- 2026-08-27 (twenty-third pack, same day) · Small correctif: Notre Mission's frieze connector rebuilt to reuse `/nos_innovations/`'s exact mechanism (a real line element + its own two dots, not the earlier `repeating-linear-gradient`/`box-shadow` approximation) - fixes dashes that overflowed past the outer circles and adds the two missing intermediate dots. Same responsive behavior too (connector hidden entirely on mobile). Notre Vision: the three middle pillars' text blocks (title/tagline/copy) are now genuinely centered under their circle (same 215px width, `margin:auto`), not just `text-align:center` inheriting past the global paragraph-left-align rule. See `source/brand/design.md`.
- 2026-08-27 (twenty-second pack, same day) · `/innovations` renamed to `/nos_innovations` (old URL 301-redirects via a Cloudflare Pages `_redirects` file; every internal link updated). Same pass: innovations filters recolored to the exact navy of the page's own dark top band (was cyan-bordered); Médico-économique gained a proper unselected gold state (previously fell back to the generic navy style); MesGardes/HepatGreen got a 1px gold border to signal they're the médico-économique family; the page's own bottom CTA button ("Nous contacter") harmonized to cyan BOPA with the header's reference hover.
- 2026-08-27 (twenty-first pack, same day, quick follow-up) · Notre Vision: the timeline connector's `top` offset (line, arrowhead, per-step dot) hadn't been recalculated when the circles shrank from 84px to 62px in the V4.1 pass - fixed to `31px` (the new circles' true half-height), and "Progrès pour tous" now aligns to the last circle's left edge (same width + centering as the icon) instead of its column's.
- 2026-08-27 (twentieth pack, same day, "V4.1") · Home reordered again: Mission → Innovations → Vision → Qui sommes-nous (Chaire d'excellence → CHB/environnement clinique → Équipe opérationnelle) → Contact → Soutenir - reverting the previous pack's order for Vision and for the Chaire/Équipe/Environnement sequence. Backgrounds largely inverted from the prior pass too: Vision back to navy, Chaire to pale, Équipe to **white** (a correctif the owner sent as a follow-up fix), Contact to pale, Soutenir to white. The header CTA's hover (darken to `--bopa-blue-1`, `translateY(-2px)`, `rgba(3,82,156,.35)` shadow) became the reference applied to "Découvrir nos projets" and "Soutenir la Chaire BOPA". Mission's and Vision's Pré/Per/Post-style circles were harmonized to `/innovations/`'s own frieze (62px, white fill, cyan border/pictogram) - `/innovations/` itself untouched, used only as the reference. See `source/brand/design.md` for the button-hover cascade lesson this surfaced.
- 2026-08-27 (nineteenth pack, same day) · `/innovations` H1 shortened to "Nos innovations" (was "Toutes nos innovations"), its title+lead+Pré/Per/Post journey moved onto a bleu-nuit band (`var(--mp-navy)`, same as Notre Mission) that stops exactly before the filters. Home: the "Nos innovations" CTA button reworded to "Découvrir nos projets" (same link/style), and the small footer arrows on the 4 homepage project vignettes removed - cards now center their content vertically so shorter descriptions don't leave a large uneven gap where the arrow used to sit. `/innovations`' own cards (never had arrows) untouched, along with its filters, categories, and the médico-économique gold.
- 2026-08-27 (eighteenth pack, same day) · Home reorganized into its final structural order: Mission → Vision → Innovations → Qui sommes-nous (Chaire d'excellence → Équipe opérationnelle → Environnement clinique) → Contact → Soutenir, with the header reordered to match. No section was redesigned - existing markup moved and renumbered, plus a chromatic rhythm applied (navy/white/pale alternating per chapter, see `source/brand/design.md`). Vision flipped from navy back to white (its fourth background in this project's history); Innovations/Soutenir unified onto the one pale token (`--color-bg-tint`); the "Faites avancer l'innovation..." CTA stayed navy inside the now-pale Soutenir. Navy value itself never touched anywhere.
- 2026-08-27 (seventeenth pack, same day, quick follow-up) · The owner flagged three CTA buttons the cyan pass had deliberately skipped (header/Hero "Nous soutenir", "Découvrir tous nos projets") over a white-text contrast concern - after being shown the tradeoff, chose full harmonization anyway. Both now `--bopa-cyan`; "Soutenir la Chaire BOPA" was already cyan via `--mp-blue-bright`.
- 2026-08-27 (sixteenth pack, same day) · Site-wide colorimetric harmonization: created `--bopa-cyan` (`#2db9e3`, the exact color of the Hero's "de demain") as the single reference for every light-blue accent, and repointed kickers/eyebrows, Mission's timeline dotted lines, and `/innovations`' icon outlines/filter borders/CTA accents to it. Deliberately left untouched: navy, logos, photos, the médico-économique gold. No layout or typography changed. See `source/brand/design.md`.
- 2026-08-17 · Axelle Piguet onboarded as a second collaborator (GitHub: axpiguet, admin access) and set up to work through her own Claude session, pushing directly to main alongside her coworker.
- 2026-08-27 · Homepage visual direction pivoted: the continuous-gradient/mesh-echo/ambient-halo system (built up over the prior week) was replaced by a flat, hard-edged, blue-only-palette system from an externally-produced ("BOPA V2") redesign, integrated after a full diff against the live project. See `source/brand/design.md` for the specifics now in force.
- 2026-08-27 (later same day) · "Notre mission" and "Nos projets phares" rebuilt again from a second externally-sourced pack ("V3"), replacing the pipeline/vignette version from earlier that day - new `.bopa-mission-v2`/`.bopa-projects-v2` components, new illustrative project visuals (`images/projets-v2/`). BOPEX dropped out of view in the process (see the open item above).
- 2026-08-27 (third pack, same day) · `/innovations` rebuilt from a third externally-sourced pack, reorganized into the owner's validated 4-category/14-project structure ("Coût de l'hépatectomie" removed, "L'évaluation de la charge cognitive du chirurgien" added), reusing `.bopa-project-card` from the homepage rather than a new card component.
- 2026-08-27 (fourth pack, same day) · `/innovations` editorial pass: all 14 titles/descriptions rewritten (two renames - "Optimiser l'allocation des greffons" → LiverMatch, the VBHC project → BOPA Value), every card made non-clickable with the arrow removed (only LiverSight's video and the Compte-rendu card's Uncovr link stay as small explicit text CTAs), and a one-off gold accent (`--color-gold`) added for the Médico-économique category only.
- 2026-08-27 (same day, quick follow-up) · Removed the two team-gallery photos below the operational team cards, at the owner's request. Hardened the gallery's `readdirSync` with a try/catch in the process (matching `team-photos`) - the folder itself disappeared once both files were gone, which would have crashed the build without the fix.
- 2026-08-27 (fifteenth pack, same day) · Context band reworked again: now full-viewport-width (dividers at 25/50/75% of the screen, escaping the site's content max-width), "exactly 2 lines" relaxed to "2-3 lines, legibility over line count" (owner's explicit reversal of the prior constraint), type raised ~25%. Header sticky behavior and anchor offsets confirmed unaffected. The CHB fragment's 2-column fallback breakpoint moved again (82rem this time) to keep it within the new 3-line ceiling.
- 2026-08-27 (fourteenth pack, same day) · "Notre vision" arrow follow-up: the line and arrowhead now share the same endpoint (15.5%) so the line stops exactly where the arrow tip is, instead of visibly continuing behind it toward the circle. Gap to "Progrès pour tous" now 28-51px depending on width (targets the owner's requested 30-45px range at a representative width, can't be exact at every width since the circle is a fixed size).
- 2026-08-27 (thirteenth pack, same day) · Context band (`#environnement`) made 4 strictly equal columns (was disproportionate, quietly compensating for the CHB fragment's longer text) with explicit 2-line text control per fragment and a smaller, band-specific type size (no longer matching Mission's lead, per the owner's explicit call). The "exactly 2 lines" promise only holds ≥1250px wide - below that the band falls back to its existing 2-column/natural-wrap layout, since no legible font size makes the CHB fragment's text fit one line at narrower widths. Flagged to the owner as a deliberate trade-off, not silently decided.
- 2026-08-27 (twelfth pack, same day) · "Notre vision"'s timeline connector changed from a dotted line to a solid directional line with a single arrowhead before "Progrès pour tous" - a narrow, surgical patch, nothing else in the section touched.
- 2026-08-27 (eleventh pack, same day) · Site-wide typographic harmonization: Notre Mission's H2 and intro metrics became the formal Niveau 1/Niveau 2 étalons (see `source/brand/design.md`), applied to the Hero title+lede, the context-band's 4 texts, the home's "Les innovations BOPA" title, and `/innovations`' own title. Follow-up same day: the Hero lede was wrapping to 4 lines instead of 3 in the ~700-1150px range with the larger harmonized type - fixed by adding a fixed rem floor to the hero's text-column/blur-zone widths (not just a bigger vw ratio), verified at every width from 700px to 1920px.
- 2026-08-27 (tenth pack, same day) · "Notre vision" rebuilt a third time (V4), this time to harmonize its metrics exactly with "Notre Mission" - same eyebrow/H2/intro sizing, same 5.25rem icon circles, same real 64px top/bottom padding (matching Mission's actual computed value, not its nominal clamp()). Class renamed `.bopa-vision` → `.bopa-vision-v4`. Mission, Innovations, and Qui sommes-nous untouched.
- 2026-08-27 (ninth pack, same day) · Header/Nous soutenir restructuring: home anchor ids renamed to match a flattened header nav exactly (`notre-mission`, `innovations-bopa`, `notre-vision`, `qui-sommes-nous`, `nous-contacter`, `nous-soutenir`); "Nous contacter" and "Nous soutenir" swapped order so soutenir is now last before the footer; the "Ils nous soutiennent" mécénat content moved out of "Qui sommes-nous" into a redesigned, richer "Nous soutenir" (3 foundation cards, 2 patron cards, donation panel), same real logos/URL as before. Header nav flattened per the owner's request, cyan CTA now points at `/#nous-soutenir` instead of the external donation link directly. Same-day follow-up: the owner asked to drop `/innovations`/`/boptech`/`/publications` from the nav entirely (pages untouched, just no longer linked from the header).
- 2026-08-27 (same day, quick follow-up) · "Notre vision" switched from its own pale-blue background to the same navy as "Notre Mission" (`--mp-navy`), at the owner's request after seeing it live - text/accent colors flipped to their light-on-dark equivalents to match.
- 2026-08-27 (eighth pack, same day) · Added the standalone "Notre vision - De l'innovation au progrès" section between "Nos innovations" and "Qui sommes-nous ?", resolving the open item flagged below (the removal pack had assumed it already existed; it didn't - this was the missing companion pack). Purely a copy/design section, no real-world facts to source: inline SVG icons only, no external assets. Mission/Nos innovations/Qui sommes-nous left untouched, per the owner's explicit scope. No "Vision" nav entry added (not requested).
- 2026-08-27 (seventh pack, same day) · The legacy "Notre approche" / "La vision : changer les pratiques du bloc opératoire" / "La méthode" (Prototyper/Codévelopper/Tester) block removed entirely - it had survived, unused-but-rendered, through several redesign passes at the end of the home. Nav's `/#approche` entry removed too (the "La Chaire" dropdown now has a single child, "Qui sommes-nous" - left as-is rather than flattened, since this pass's scope was explicitly deletion-only). `#contact` ("Nous contacter") is now the page's last editorial section before the footer. While cleaning nearby id-scoped CSS, also found and fixed a real, previously-undetected bug (see `source/brand/design.md`'s sixth "id beats class" variant): stray leftover rules were forcing "Qui sommes-nous" to a solid navy block and "Nous soutenir" to a light background since each section's own launch - both now render with their intended colors.
- 2026-08-27 (sixth pack, same day) · "Nous soutenir" + "Nous contacter" rebuilt as two short, separate sections (`.bopa-support`/`.bopa-contact`) replacing the old single dark "donate" section, positioned right after the "Qui sommes-nous" chapter and ahead of "Notre approche". The owner explicitly chose to drop the old section's 5 impact cards and stat row (1.6M€/14 projets/8 thèses/2 logiciels/2 start-up) from display rather than keep or relocate them, to match the reference exactly. Real facts reused throughout (see the contact-facts line above); the contact form keeps its exact prior Web3Forms mechanism, gaining a real Sujet dropdown (the site's actual 3 contact reasons) and a consent checkbox.
- 2026-08-27 (fifth pack, same day) · "Qui sommes-nous" rebuilt as a rich 4-part chapter (`.about-v2`, own `<style>` block in `index.astro`) placed right after "Nos projets phares", replacing the old short qui-sommes-nous text block and folding the old standalone `#equipe` section into it as its "03" subsection (`#equipe` anchor now lives on that inner subsection - nav links to both still work by id). This re-homes BOPEX (see the now-removed open item above) as one of two "réalisations nées de BOPA, aujourd'hui autonomes" alongside BOPTECH, each linking to its real destination (BOpEx: `aphp.fr/bopex`, no internal page exists; BOPTECH: the site's own `/boptech`). Team roster shown: Pr Éric Vibert, Charles Hellec, Elie Farah, Axelle Piguet - the owner explicitly chose to keep this real 4 rather than the pack's suggested swap-in of an unverified fifth person ("Claire Barlette") who has no real photo/LinkedIn/credentials anywhere in the project. The owner supplied a real CHB building photo through `source/inbox/` this session, now at `site/public/photos/centre-hepato-biliaire.png` (original in `source/brand/assets/photos/`) - the section's frontmatter auto-detects it by filename the same way team headshots do, so a future replacement just needs the file swapped, no code change.
